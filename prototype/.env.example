# Copy to .env and adjust as needed. docker compose reads .env automatically.

# Host port the API is published on (container always listens on 8080 internally).
BACKEND_PORT=4000

# Only needed when running docker-compose.https.yml (Caddy HTTPS overlay).
DOMAIN=stream.example.com
